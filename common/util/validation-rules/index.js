let max = require('./max');
let min = require('./min');
let size = require('./size');
let required = require('./required');

exports = {
  max: max.max,
  min: min.min,
  required: required.required,
  size: size.size
};
